import LoadingLabView from "@/components/loading/LoadingLabView";

// TEMP route for comparing loading variants — remove once one is chosen.
export default function LoadingLabPage() {
  return <LoadingLabView />;
}
